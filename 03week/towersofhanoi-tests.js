// It should only let a smaller number on top a bigger number,
// isLegal would fail if {a: [4, 3, 2], b: [1], c: []} user tried to move 2 on top 1


// It should be over when the numbers are shown high to low on c,
// checkForWin would look like {a: [], b: [], c: [4, 3, 2, 1]}


//